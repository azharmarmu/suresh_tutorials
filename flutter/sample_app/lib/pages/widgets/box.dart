import 'package:flutter/material.dart';

class BoxRed extends StatelessWidget {
  const BoxRed({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      color: Colors.red,
    );
  }
}

class BoxGreen extends StatelessWidget {
  const BoxGreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      color: Colors.green,
    );
  }
}



class BoxBlue extends StatelessWidget {
  const BoxBlue({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 100,
      height: 100,
      color: Colors.blue,
    );
  }
}

